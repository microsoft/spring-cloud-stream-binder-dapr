dapr:
  client.pubsub.subscriptions:
    - pubsubName: eventhubs-pubsub
      topic: topic
spring:
  cloud:
    stream:
      function:
        definition: supply
      bindings:
        supply-out-0:
          destination: topic
      dapr:
        bindings:
          supply-out-0:
            producer:
              pubsubName: eventhubs-pubsub
